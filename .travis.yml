dst: bionic
language: python
services:
  - docker

env:
  global:
    - DB_HOST=db
    - DB_PORT=3306

before_install:
  - sudo apt-get update -y
  - sudo apt-get install netcat -y
  - mv env.travis .env
  - docker-compose up --build -d
script:
  - bash ./run_tests.sh
